export function getImageUrl(path) {
  // Cette fonction pourrait être étendue pour gérer différentes sources d'images,
  // le redimensionnement, etc.
  return path;
}

export function generateBlurHash(imageUrl) {
  // Pour une future implémentation de placeholder d'image
  return null;
}